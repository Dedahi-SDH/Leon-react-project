export default function Modal() {
  return (
    <div id="modal">
      <div id="modal-content">
        Hi from Modal, keep coding, keep pushing and allah with us so we are not
        afraid
      </div>
    </div>
  );
}
